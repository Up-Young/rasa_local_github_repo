#on:
#  push:
#    branches:
#      - main
#    paths:
#    - 'actions/**'

#jobs:
#  build_and_deploy:
#    runs-on: ubuntu-latest
#    name: Build Action Server image and upgrade Rasa X deployment
#    steps:
#    - name: Checkout repository
#      uses: actions/checkout@v2
#
#    - id: action_server
#      name: Build an action server with a custom actions
#      uses: actions/checkout@v2
#      # Full list of parameters: https://github.com/RasaHQ/action-server-gha/tree/master#input-arguments
#      with:
#        docker_image_name: 'yangyoungyoung/rasa_local_docker_repo'
#        docker_registry_login: ${{ secrets.DOCKER_HUB_LOGIN}}
#        docker_registry_password: ${{ secrets.DOCKER_HUB_PASSWORD }}
#        # More details about github context:
#        # https://docs.github.com/en/actions/reference/context-and-expression-syntax-for-github-actions#github-context
#        #
#        # github.sha - The commit SHA that triggered the workflow run
#        docker_image_tag: ${{ github.sha }}
##        requirements_file: './requirements.txt'

on:
  push:
    branches:
      - main
    paths:
    - 'actions/**'

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    name: Build Action Server image and upgrade Rasa X deployment
    steps:
    - name: Checkout repository
      uses: actions/checkout@v2
#
    - name: Build an action server
      uses: RasaHQ/rasa-action-server-gha@main
      with:
        docker_image_name: 'rasahq/action-server-example'
        # More details on how to use GitHub secrets:
        # https://docs.github.com/en/actions/configuring-and-managing-workflows/creating-and-storing-encrypted-secrets
        docker_registry_login: ${{ secrets.DOCKER_HUB_LOGIN }}
        docker_registry_password: ${{ secrets.DOCKER_HUB_PASSWORD }}
        # More details about github context:
        # https://docs.github.com/en/actions/reference/context-and-expression-syntax-for-github-actions#github-context
        docker_image_tag: ${{ github.sha }}

#    - name: Build an action server
#      uses: ./
#      with:
#        actions_directory: 'Up-Young/rasa_local_github_repo/actions'
##        docker_registry: 'yangyoungyoung/rasa_local_docker_repo'
#
#        docker_image_name: 'yangyoungyoung/rasa_local_docker_repo'
#        # More details on how to use GitHub secrets:
#        # https://docs.github.com/en/actions/configuring-and-managing-workflows/creating-and-storing-encrypted-secrets
#        docker_registry_login: ${{ secrets.DOCKER_HUB_LOGIN }}
#        docker_registry_password: ${{ secrets.DOCKER_HUB_PASSWORD }}
#        # More details about github context:
#        # https://docs.github.com/en/actions/reference/context-and-expression-syntax-for-github-actions#github-context
#        docker_image_tag: ${{ github.sha }}
#        requirements_file: 'actions/requirements.txt'
#        # ...

